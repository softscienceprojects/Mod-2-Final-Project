<div class="row">
    <div class="col-12">
        <h1><%= @category.name %></h1>
        <p>All events</p>
    </div>
</div>



<div class="flex-container">
    <% @category.events.each do |event| %>
      <div> 
      <h4> <%= link_to event.title, event_path(event) %></h4>
      </div>

    <% end %>
</div>


